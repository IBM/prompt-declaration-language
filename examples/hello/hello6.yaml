description: Hello world showing call out to python code
prompts:
- 'Hello, '
- lan: python
  code:
  - |
    import random
    import string
    result = random.choice(string.ascii_lowercase)
- "!\n"
