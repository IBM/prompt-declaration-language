description: Hello world showing call out to python code with condition
document:
- 'Hello! '
- lan: python
  code: |
    import random
    result = random.choice([False, True])
  def: QUESTION
  show_result: false
- then:
  - |
    How are you?
  if:
    ends_with: {'arg0': { 'get': 'QUESTION' }, 'arg1': 'true' }
